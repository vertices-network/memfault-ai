# Claude Instructions

For Memfault issue analysis workflows, use the skill instructions in:

`memfault-issue-analyzer/SKILL.md`

Load additional skill references from:

`memfault-issue-analyzer/references/`

The Python CLI and supporting modules live at the repository root. Run tool commands from this root directory unless instructed otherwise.

Do not read or print `.env`, `analysis_results/`, `venv/`, caches, or other ignored runtime/private files unless explicitly requested.
